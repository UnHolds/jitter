//this functions tests if the parameter are accessed in the right way (stack align)
fun test1(a, b, c, d, e, f, g, h){
    return a + b + c + d + e + f + g + h;
}

fun main() {
    a = test1(1, 2, 3, 4, 5, 6, 7, 8);
    println_num(a);
    return a;
}
